<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Total_data_backup_count__c</fullName>
    <externalId>false</externalId>
    <label>Backup seats</label>
    <description>This is the total number of backup seats that the user has assigned to their account. We don't currently enforce that the team doesn't breach this limit, so it's something that we should check at renewal time.</description>
    <inlineHelpText>This is the total number of backup seats that the user has assigned to their account. We don't currently enforce that the team doesn't breach this limit, so it's something that we should check at renewal time.</inlineHelpText>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
